name: Actionlint with reviewdog

on:
  push:
    branches:
      - main
      - master
  pull_request:
  merge_group:

jobs:
  actionlint:
    name: Actionlint with reviewdog
    uses: book000/templates/.github/workflows/reusable-actionlint.yml@224a681f582a2bb48934752864be4f4565ca0b23 # master
